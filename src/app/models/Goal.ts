export interface Goal {
    id ?: number,
    name?:string,
    description?:string,
    deadline ?:Date,
    weightage ?: number,
    comments ?: string,
    status ?: string,
    personal ?: number
}